version: '2'

services:
  app:
    image: jeremiasabel/agro:latest
    container_name: agro
    ports:
      - "4200:4200"

  backend:
    image: jeremiasabel/agroback:latest
    container_name: agroback
    ports:
      - "8080:8080"
